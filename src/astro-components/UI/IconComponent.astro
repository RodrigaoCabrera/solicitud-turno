---
interface Props {
  iconPath: string;
}
const { iconPath } = Astro.props;

const Component = await import(iconPath).then((module) => module.default);
---

<Component width="15" height="15" viewBox="0 0 15 15" fill="none" />
